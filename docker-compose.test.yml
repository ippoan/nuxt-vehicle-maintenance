services:
  test-db:
    image: postgres:16
    environment:
      POSTGRES_PASSWORD: test
    ports:
      - "54322:5432"
    volumes:
      - ./tests/fixtures/init_local_db.sql:/docker-entrypoint-initdb.d/01-init.sql
    tmpfs:
      - /var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 3s
      timeout: 3s
      retries: 10

  minio:
    # MinIO 公式 image (registry を問わず) が匿名 pull 不可になったため、
    # 匿名 pull できる凍結アーカイブに差し替え (Refs #10)。
    image: bitnamilegacy/minio:2025.7.23-debian-12-r5@sha256:6dabb4a2088c9a79908de3bc05f4586c23ad2182c8908e7e3acbf61c1467fb20
    environment:
      MINIO_ROOT_USER: minioadmin
      MINIO_ROOT_PASSWORD: minioadmin
      MINIO_DEFAULT_BUCKETS: alc-test
    healthcheck:
      test: ["CMD", "mc", "ready", "local"]
      interval: 3s
      timeout: 3s
      retries: 10

  api-migrate:
    image: ${API_IMAGE:-ghcr.io/ippoan/rust-alc-api:dev}
    depends_on:
      test-db:
        condition: service_healthy
    environment:
      DATABASE_URL: postgresql://postgres:test@test-db:5432/postgres?options=-c search_path=alc_api
    entrypoint: ["migrate"]
    restart: "no"

  db-seed:
    image: postgres:16
    depends_on:
      api-migrate:
        condition: service_completed_successfully
    volumes:
      - ./tests/fixtures/seed.sql:/seed.sql
    entrypoint: ["sh", "-c"]
    # ★ -v ON_ERROR_STOP=1 は必須。psql は既定でエラーを飛ばして続行し **exit 0 で
    # 終わる**ため、これが無いと seed が 1 行も入らなくても db-seed は成功扱いになり、
    # api が空の DB で起動して integration test だけが理由不明で落ちる
    # (実際に踏んだ: maintenance_* が無い古い backend image で seed 全滅、exit 0)。
    command:
      - PGPASSWORD=test psql -v ON_ERROR_STOP=1 -h test-db -U postgres -f /seed.sql
    restart: "no"

  api:
    image: ${API_IMAGE:-ghcr.io/ippoan/rust-alc-api:dev}
    depends_on:
      db-seed:
        condition: service_completed_successfully
      minio:
        condition: service_healthy
    ports:
      - "18080:8080"
    environment:
      DATABASE_URL: postgresql://postgres:test@test-db:5432/postgres?options=-c search_path=alc_api
      JWT_SECRET: test-jwt-secret-for-integration
      GOOGLE_CLIENT_ID: dummy-google-client-id
      GOOGLE_CLIENT_SECRET: dummy-google-client-secret
      OAUTH_STATE_SECRET: dummy-oauth-state-secret
      STORAGE_BACKEND: r2
      R2_BUCKET: alc-test
      R2_ACCOUNT_ID: test
      R2_ACCESS_KEY: minioadmin
      R2_SECRET_KEY: minioadmin
      R2_ENDPOINT: http://minio:9000
      R2_PATH_STYLE: "1"
      RUST_LOG: debug
